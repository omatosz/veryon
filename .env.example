# PostgreSQL / TimescaleDB
POSTGRES_USER=socadmin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=socsiem
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Backend
BACKEND_PORT=8000

# API auth (Fase 6) -- gere um secret proprio, ex: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# Threat Intel (Fase 5) -- crie contas gratuitas e cole suas chaves aqui
ABUSEIPDB_API_KEY=
VIRUSTOTAL_API_KEY=
OTX_API_KEY=

# Analise de API
# Chave que o gateway de um cliente usa pra mandar log de acesso pro endpoint
# POST /ingest/api-logs, no cabecalho X-Veryon-Key. Deixar vazio desliga a
# ingestao: sem chave, o endpoint recusa tudo em vez de aceitar de qualquer um.
# Gere a sua com: python -c "import secrets; print(secrets.token_hex(20))"
INGEST_API_KEY=
